# Project Rules & Customizations

## Live Deployment Rule
- **NEVER PUSH DIRECTLY TO `main` BRANCH (LIVE PRODUCTION SITE)**.
- All development, testing, and commits must be done on the `dev` branch or locally.
- Only push to `main` when the user explicitly instructs to deploy changes to the live site.
